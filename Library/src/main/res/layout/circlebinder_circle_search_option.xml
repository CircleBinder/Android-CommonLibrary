<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <EditText
        android:id="@+id/circle_search_keyword"
        android:layout_toLeftOf="@+id/circle_search_option"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:maxLines="1"
        android:inputType="textAutoComplete"
        android:imeOptions="actionSearch"
        android:hint="@string/circlebinder_app_circle_search_hint"
        />

    <!--
    <ImageView
        android:id="@+id/circle_search_option"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_menu_moreoverflow"
        android:contentDescription="@string/circlebinder_app_circle_search_option_accessibility"
        />
    -->

</RelativeLayout>